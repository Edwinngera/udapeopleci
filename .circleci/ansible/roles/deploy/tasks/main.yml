---
  - name: Copy files
    become: true
    copy:
      src: '/backend/artifact.tar.gz'
      dest: /home/ubuntu/artifact.tar.gz
    remote_src: true
  - name: Unarchive
    become: true
    shell : |
        cd /home/ubuntu/
        tar xvzf artifact.tar.gz -C .
  - name: "run server"
    become: true
    shell : |
        cd /home/ubuntu/
        npm install
        pm2 stop default
        pm2 start npm -- start